Return JSON only: `{"schemaVersion":1,"caseId":"documentation","result":"...","findings":[],"completed":true}`.

Rewrite exactly this instruction in active voice, preserving command and meaning, ending with one period:

`Repository inspection delegation must be preceded by running agnt route.`
